Breakpoint Web
--------------

After you clone this repository, you should open a Powershell command line in the folder in which you put the project and run the following two commands.

```
npm install
jspm install
```

After that, you can start Breakpoint using the following command.

```
gulp serve
```